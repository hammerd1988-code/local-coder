# Server Configuration
NODE_ENV=development
PORT=3001

# Data Directory (where database and uploads are stored)
DATA_DIRECTORY=/home/app/data

# Casper model defaults (optional; UI-saved settings take precedence)
# MODEL_PROVIDER=openrouter
# MODEL_NAME=qwen/qwen3.8-27b
# OPENROUTER_API_KEY=

# Production Settings
# NODE_ENV=production
# PORT=4000
